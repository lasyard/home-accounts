# Bugs

这个文件只能由人类手动修改，AI 勿动。

以下复选框列表的每一项代表一个缺陷。每个缺陷前面有一个数字是它的编号，随后的文字为它的标题。每个缺陷的详细描述在此列表的后面的每个二级标题下面，编号与列表中的编号对应。

当被要求修复某个缺陷时，如果对应的复选框已选中，说明此缺陷已修复，应该忽略并报告“缺陷 XXXX 已修复，放弃动作”。

先读 `memo.md` 以获取关键信息，如果需要的信息找不到，再尝试从源码中寻找。
