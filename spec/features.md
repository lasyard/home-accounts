# Features

这个文件只能由人类手动修改，AI 勿动。

以下复选框列表的每一项代表一个特性。每个特性前面有一个数字是它的编号，随后的文字为它的标题。特性可以包含子列表，代表子特性。实现某个特性的时候需按次序实现各个子特性。每个特性的详细描述在此列表的后面的每个二级标题下面，编号与列表中的编号对应，子特性则放在对应的三级标题下面。

当被要求实现某个特性或子特性时，如果对应的复选框已选中，说明此特性已实现，应该忽略并报告“特性 XXXX 已实现，放弃动作”。

先读 `memo.md` 以获取关键信息，如果需要的信息找不到，再尝试从源码中寻找。

- [x] 1. 自动备份文件
- [ ] 2. 导入 CSV 文件
  - [x] 2.1. 读入 CSV 文件
  - [ ] 2.2. 字段映射
  - [ ] 2.3. 合并到 data 段

## 1. 自动备份文件

保存文件时，将原文件重命名为“原文件名_日期_时间.扩展名”的形式作为备份，以原文件名保存新的内容。

要求：

1. 日期格式为 YYYY-MM-DD, 时间格式为 hhmmss
2. 打开文件后如果没有保存，则不生成备份
3. 备份文件的个数控制在 n 个以内，如果同目录下已经有 n 个及以上的备份文件，把较旧的那些删除（n 定义为 5）

## 2. 导入 CSV 文件

导入指定 CSV 文件中的数据，保存到 Section `import`, 显示在 Import 面板中。

CSV 文件的格式：

1. 第一行为每一列的标题
2. 分隔符为 `,`

要求：

1. 当前有文档打开时生效
2. 由菜单“edit::import”触发

执行的动作：

1. 如果当前文档包含 Section, 则读取这个 Section, 否则弹出一个文件对话框让用户选择一个 CSV 文件并读取这个文件
2. 添加/切换到 Import 面板，以表格方式显示读到的内容

## 3. 字段映射

导入 CSV 文件时：

- 如果存在 Section `conf/import_col_map`
  1. 读取这个 Section 的内容，得到 data 字段的编号和对应的 TITLES 列表
  2. 在读取 CSV 文件的第一行后，分析每一列的标题
     - 如果列标题在某个 TITLES 列表中，则记住这一列对应的 data 字段编号（以便将数据合并到 data 段）并进一步确定它的类型
     - 如果列标题不在任何 TITLES 列表中，则这一列对应的 data 字段编号为 -1（表示无效），类型为 `CT_STR`
  3. 以每一列确定的类型构造 parser 并读入文件的剩余部分
- 如果不存在，所有列的 data 字段编号为 -1, 类型为 `CT_STR`

在 Import 面板中显示时，在所有数据行前加两行：

1. 原 CSV 文件每一列的标题
2. 每一列对应的 data 字段的名称
